let attempt = 0; // Init
while (attempt < 3) { // Condition
    console.log(attempt);
    attempt++; // Updation
}

let modi = 1;
while (modi <= 15) { // 1 to 15,Times -> 

    console.log("Modi will do 15+ years");
    modi++;

} // diffrence between for and while loop is that for loop is used when we know the number of iterations, while loop is used when we don't know the number of iterations.
